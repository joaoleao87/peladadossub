import { useState, type FormEvent } from 'react'
import { Navigate } from 'react-router-dom'
import { supabase } from '../lib/supabase'
import { useAuth } from '../auth/AuthContext'

export function AuthPage() { const { session } = useAuth(); const [mode, setMode] = useState<'login' | 'signup' | 'reset'>('login'); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(''); if (session) return <Navigate to="/" replace />
  async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setBusy(true); setMessage(''); const form = new FormData(e.currentTarget), email = String(form.get('email')), password = String(form.get('password') || ''), nome = String(form.get('nome') || ''); try { if (mode === 'login') { const { error } = await supabase.auth.signInWithPassword({ email, password }); if (error) throw error } else if (mode === 'signup') { const { error } = await supabase.auth.signUp({ email, password, options: { data: { nome } } }); if (error) throw error; setMessage('Cadastro feito. Confira seu e-mail para confirmar.') } else { const { error } = await supabase.auth.resetPasswordForEmail(email, { redirectTo: `${location.origin}/perfil` }); if (error) throw error; setMessage('Enviamos o link de recuperação.') } } catch (err) { setMessage(err instanceof Error ? err.message : 'Não foi possível continuar.') } finally { setBusy(false) } }
  return <div className="auth-page"><div className="auth-mark">SUB</div><h1>PELADA<br/><em>DOS SUB</em></h1><p>Organização, competição e resenha.</p><form onSubmit={submit}>{mode === 'signup' && <label>Nome<input name="nome" required autoComplete="name"/></label>}<label>E-mail<input name="email" type="email" required autoComplete="email"/></label>{mode !== 'reset' && <label>Senha<input name="password" type="password" minLength={6} required autoComplete={mode === 'login' ? 'current-password' : 'new-password'}/></label>}<button disabled={busy}>{busy ? 'AGUARDE…' : mode === 'login' ? 'ENTRAR' : mode === 'signup' ? 'CRIAR CONTA' : 'ENVIAR LINK'}</button>{message && <div className="form-message">{message}</div>}</form><div className="auth-links">{mode !== 'login' && <button onClick={() => setMode('login')}>Já tenho conta</button>}{mode !== 'signup' && <button onClick={() => setMode('signup')}>Criar conta</button>}{mode !== 'reset' && <button onClick={() => setMode('reset')}>Esqueci a senha</button>}</div></div> }
