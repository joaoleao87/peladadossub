import { useAuth } from '../auth/AuthContext'
import { Empty, ErrorState, Spinner } from '../components/Ui'
import { useLoad } from '../hooks/useLoad'
import { nextPelada, participants } from '../lib/api'

export function ListPage() { const { profile } = useAuth(); const state = useLoad(async () => { const game = await nextPelada(); return { game, list: game ? await participants(game.id) : [] } }, []); if (state.loading) return <Spinner/>; if (state.error) return <ErrorState message={state.error} retry={state.reload}/>; if (!state.data?.game) return <section><h1>Lista</h1><Empty title="Sem lista aberta"/></section>; const confirmed = state.data.list.filter(p => ['confirmado','presente'].includes(p.status)), waiting = state.data.list.filter(p => p.status === 'espera'); const group = (title: string, items: typeof confirmed) => <div className="list-group"><div className="list-heading"><h2>{title}</h2><span>{items.length}</span></div>{items.length ? items.map((p, i) => <div className={`player ${p.user_id === profile?.id ? 'me' : ''}`} key={p.id}><b>{i+1}</b><div className="avatar">{p.profile?.foto_url ? <img src={p.profile.foto_url} alt=""/> : (p.profile?.apelido || p.profile?.nome || '?')[0]}</div><span>{p.profile?.apelido || p.profile?.nome}{p.user_id === profile?.id && <small> VOCÊ</small>}</span></div>) : <Empty title="Ninguém por aqui"/>}</div>; return <section><p className="eyebrow">{new Date(`${state.data.game.data}T12:00`).toLocaleDateString('pt-BR')} • {state.data.game.horario.slice(0,5)}</p><h1>Lista da pelada</h1>{group('Confirmados', confirmed)}{group('Lista de espera', waiting)}</section> }
